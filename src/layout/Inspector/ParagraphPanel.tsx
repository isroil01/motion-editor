/**
 * ParagraphPanel — deprecated.
 * Paragraph controls have been merged into the unified Text inspector panel (`CharacterPanel`).
 * Re-exported here for backward compatibility with existing tests and layouts.
 */
import { CharacterPanel } from './CharacterPanel';

export const ParagraphPanel = CharacterPanel;
export default CharacterPanel;
